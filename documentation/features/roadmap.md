# Roadmap

## Next release
- Make Kiso available as a GitHub action.
- Add a Kiso parameter to specify the language of the generated HTML files (`<html lang="en">`, date format, text
  in pages, etc.).

## Future releases
- Generate llms.txt file based on a parameter in the Markdown files.
- Add specific parameters to customize the generated HTML files (e.g., title, description, keywords, etc.).
- Add support for specific themes and templates for the generated HTML files.