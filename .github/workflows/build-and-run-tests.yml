name: Build and run tests

on:
  push:
    tags-ignore:
      - '*'
    branches-ignore:
      - production

jobs:

  build_and_run_tests:
    name: Build and run tests
    runs-on: ubuntu-latest
    permissions:
      contents: read
      actions: write
    steps:

      - name: Retrieve the sources
        uses: actions/checkout@v7

      - name: Set up environment
        uses: ./.github/actions/setup

      - name: Install system dependencies for PNG tests
        run: |
          sudo apt update
          sudo apt install -y librsvg2-bin inkscape
          cargo install resvg

      - name: build and run tests
        run: |
          just test